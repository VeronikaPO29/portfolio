for i in range(1, 6):
    print('*' * i)

name = input("Введите имя:")
print('*' * (len(name) + 4))
print('*' + name + '*')
print('*' * (len(name) + 4))

number = int(input("Введите число n: "))
print("Сумма от 1 до", number, "=", sum(range(1, number + 1)))
